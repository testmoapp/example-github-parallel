const chai = require('chai');
const assert = chai.assert;

describe('reports', function () {
    describe('generate', function () {
        it('should generate images', function () {
            assert.isTrue(true);
        });

        it('should generate statistics', function () {
            assert.isTrue(true);
        });

        it('should generate dashboard', function () {
            assert.isTrue(true);
        });

        it('should generate review', function () {
            assert.isTrue(true);
        });

        it('should generate overview', function () {
            assert.isTrue(true);
        });
    });
});
