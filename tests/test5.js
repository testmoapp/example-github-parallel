const chai = require('chai');
const assert = chai.assert;

describe('auth', function () {
    describe('login', function () {
        it('should login with email', function () {
            assert.isTrue(true);
        });

        it('should login with user', function () {
            assert.isTrue(true);
        });

        it('should login with 2fa', function () {
            assert.isTrue(true);
        });

        it('should login with oauth', function () {
            assert.isTrue(true);
        });

        it('should fail login with wrong password', function () {
            assert.isTrue(true);
        });
    });
});
