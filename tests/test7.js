const chai = require('chai');
const assert = chai.assert;

describe('admin', function () {
    describe('manage', function () {
        it('should manage users', function () {
            assert.isTrue(true);
        });

        it('should manage reports', function () {
            assert.isTrue(true);
        });

        it('should manage integrations', function () {
            assert.isTrue(true);
        });

        it('should manage backups', function () {
            assert.isTrue(true);
        });

        it('should manage logins', function () {
            assert.isTrue(true);
        });

        it('should manage schedule', function () {
            assert.isTrue(true);
        });
    });
});
