const chai = require('chai');
const assert = chai.assert;

describe('auth', function () {
    describe('register', function () {
        it('should register with email', function () {
            assert.isTrue(true);
        });

        it('should register from domain', function () {
            assert.isTrue(true);
        });

        it('should register from link', function () {
            assert.isTrue(true);
        });
    });
});
