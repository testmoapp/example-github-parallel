const chai = require('chai');
const assert = chai.assert;

describe('reports', function () {
    describe('schedule', function () {
        it('should schedule today', function () {
            assert.isTrue(true);
        });

        it('should schedule weekly', function () {
            assert.isTrue(true);
        });

        it('should schedule every month', function () {
            assert.isTrue(true);
        });

        it('should schedule 10:00am', function () {
            assert.isTrue(true);
        });
    });
});
